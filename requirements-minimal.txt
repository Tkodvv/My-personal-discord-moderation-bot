# Minimal requirements for production deployment
# Use this file if the full requirements.txt causes issues

# Core Discord.py (essential)
discord.py>=2.6.0,<3.0.0
aiohttp>=3.10.0,<4.0.0
python-dotenv>=1.0.1,<2.0.0

# Voice support (essential for Discord)
PyNaCl>=1.5.0,<2.0.0

# Web server (essential for Render)
flask>=3.1.0,<4.0.0

# HTTP client (used by your bot)
httpx>=0.28.0,<1.0.0

# Basic utilities
requests>=2.32.0,<3.0.0
typing-extensions>=4.12.0
